class TransModel {
  double expense;
  String title;
  DateTime? transDate;
  bool isAdded;

  TransModel({
    required this.expense,
    required this.title,
    required this.transDate,
    required this.isAdded,
  });
}
